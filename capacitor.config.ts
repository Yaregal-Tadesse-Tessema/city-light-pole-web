import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.citylightpole.management',
  appName: 'City Light Pole Management',
  webDir: 'dist'
};

export default config;
